CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT( osg_basic )

FIND_PACKAGE(OpenSceneGraph REQUIRED osgQt osgDB osgGA osgViewer osgUtil osgSim osgText)
INCLUDE_DIRECTORIES(${OPENSCENEGRAPH_INCLUDE_DIRS})

ADD_EXECUTABLE(osg_basic  test.cpp)
TARGET_LINK_LIBRARIES(osg_basic ${OPENSCENEGRAPH_LIBRARIES})